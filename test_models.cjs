const { GoogleGenAI } = require("@google/genai");
const ai = new GoogleGenAI();
async function run() {
  try {
    // try to just fetch the list of models if we can 
    // wait, I might not have GEMINI_API_KEY locally in this script
  } catch(e) {}
}
run();
